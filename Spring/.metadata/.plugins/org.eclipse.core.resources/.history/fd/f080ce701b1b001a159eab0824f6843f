package chap3;

import org.springframework.stereotype.Component;

@Component    //객체화  'worker' 라는 이름으로 컨테이너에 저장됨.
public class Worker{
   public void word (WorkUnit unit) {
      System.out.println(this + ":work :" + unit);
   }
}